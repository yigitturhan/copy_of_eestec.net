module.exports = {
  content: [
    "./index.html",        // Include the HTML file in the `public` folder
    "./src/**/*.{js,jsx}", // Include all JS and JSX files in the `src` folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
